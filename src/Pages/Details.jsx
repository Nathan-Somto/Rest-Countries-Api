import React from 'react';
function Details() {
    // make use  of the use fetch hook to get data
    //use the url prop in the url for use fetch hook
    // for the border component change the url to update the details page
    return(
        <div>
            <h1>Details Page</h1>
        </div>
    )
    
}

export default Details;